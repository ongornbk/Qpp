ImportPackage("conio")
ImportPackage("system")
SetConsoleTitle("Shutdown")
Print("Hours to shutdown : ")
local hours = GetInput()
Print("Minutes to shutdown : ")
local minutes = GetInput()
Print("Seconds to shutdown : ")
local seconds = GetInput()
Sleep(tonumber(seconds+(60*minutes)+(3600*hours)),true)
Shutdown()