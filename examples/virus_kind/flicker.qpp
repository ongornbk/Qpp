import_as("windows","win")

local ptr0,ptr1 = win.GetForeground()
win.Show(ptr0,ptr1,false)
local ptr2,ptr3 = win.GetForeground()
win.Show(ptr0,ptr1,true)

if ptr0 == ptr2 and ptr1 == ptr3
then
import("conio")
conio.Println("Something went wrong")
conio.Pause()
os.exit()
end

while true
do
    win.Show(ptr0,ptr1,true)
    win.Show(ptr2,ptr3,true)
end

::EXIT::