import("conio")
import("math")
conio.SetTitle("Pixels")

local ptr = 0

for x = 0,500,1
do

ptr = realloc(ptr,x * 8)

conio.DrawPixel(x,5,math.ToColor(ptr,get(ptr,0),0),1)

end

free(ptr)

conio.Pause()
