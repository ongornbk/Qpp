import("conio")
--ImportPackage("math")
conio.SetTitle("Pixels")
conio.Pause()
for x = 0,1600,2 
do
   for y = 0,900,2
   do
   --local r = RandomInteger(0,255)
   --ConsoleDrawPixel(x,y,ToColor(x,y,r),5)
   conio.DrawPixel(x,y,x*y*(x-y),1)
   end
end
conio.Pause()
