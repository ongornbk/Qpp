ImportPackage("conio")
ImportPackage("system")
ImportPackage("windows")
SetConsoleTitle("Closing stuff...")
local fi,se = GetForegroundWindow()
PickWindow(fi,se)
local p = GetWindowProcessID()
local r = OpenProcess(p)
Println(tostring(r))
Print("Process ID: ")
Println(tostring(p))
Pause()