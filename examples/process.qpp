import("conio")
import("system")
import("windows")
conio.SetTitle("Process...")
local hwnd = windows.GetForeground()
local p = windows.GetProcessId(hwnd)
--local r = OpenProcess(p)
----Println(tostring(r))
conio.Print("Process ID: ")
conio.Println(tostring(p))
conio.Pause()